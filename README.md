# grade-10-tech-summative-make-a-game-\

to do:

FIX THIS

def input(key1):
   if key1 == 'escape':
        result = tkinter.messagebox.askyesno("Quit", "Are you sure you want to quit?")
        if result:
            app.quit()
        else:
            pass
        
